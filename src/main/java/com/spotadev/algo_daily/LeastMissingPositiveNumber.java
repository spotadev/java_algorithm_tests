package com.spotadev.algo_daily;

/**
 * PROBLEM STATEMENT:
 * Given an unsorted array ot integers, e.g. [0, 3, -1, -2, 1], if the arrays are sorted [-2, -1, 0, 1, 3,], there is
 * a given range from the minimum to the maximum element, in this case rnage (-2, 3)
 *
 */
public class LeastMissingPositiveNumber {
}
